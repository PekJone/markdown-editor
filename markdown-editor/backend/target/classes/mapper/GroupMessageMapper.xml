<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.markdown.editor.mapper.GroupMessageMapper">

    <resultMap id="BaseResultMap" type="com.markdown.editor.entity.GroupMessage">
        <id column="id" property="id" />
        <result column="group_id" property="groupId" />
        <result column="user_id" property="userId" />
        <result column="content" property="content" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <select id="selectByGroupId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT id, group_id, user_id, content, created_at
        FROM group_messages
        WHERE group_id = #{groupId}
        ORDER BY created_at ASC
    </select>

    <select id="countByGroupId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM group_messages WHERE group_id = #{groupId}
    </select>

    <select id="countUnreadByGroupIdAndTime" parameterType="map" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM group_messages 
        WHERE group_id = #{groupId} 
          AND created_at > #{lastReadTime}
    </select>

</mapper>