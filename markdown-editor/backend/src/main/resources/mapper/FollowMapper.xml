<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.markdown.editor.mapper.FollowMapper">

    <select id="countFollowersByDateRange" resultType="map">
        SELECT DATE(f.created_at) as date, COUNT(DISTINCT f.follower_id) as count
        FROM follow f
        WHERE f.following_id = #{userId}
          AND f.created_at BETWEEN #{startTime} AND #{endTime}
        GROUP BY DATE(f.created_at)
        ORDER BY date ASC
    </select>

</mapper>